interface:
  display_name: "resume apply"
  short_description: "Build a reviewed job queue and batch-submit safe applications"
  default_prompt: "Use $resume-apply to shortlist China mainland jobs for my resume, build a reviewed application queue, and batch-submit safe jobs after I confirm."

policy:
  allow_implicit_invocation: true
